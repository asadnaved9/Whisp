<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Whisp</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="min-h-screen bg-zinc-900 text-white flex items-center justify-center">
    <div class="w-full max-w-md bg-zinc-900 p-10 rounded-lg ">

        <h3 class="text-3xl font-semibold mb-8 text-center">Login Your Account</h3>

        <form action="/login" method="post" class="flex flex-col gap-4">
            
            <input
                class="px-3 py-2 outline-none bg-transparent border border-zinc-600 rounded-md"
                type="email"
                placeholder="Email"
                name="email"
            >
            
            <input
                class="px-3 py-2 outline-none bg-transparent border border-zinc-600 rounded-md"
                type="password"
                placeholder="Password"
                name="password"
            >
            
            <input
                class="px-5 py-2 bg-blue-600 cursor-pointer rounded-md"
                type="submit"
                value="Login Account"
            >
            <p class="text-zinc-500 text-center">Don't have an Account?</p>
            <input
                class="px-5 py-2 bg-emerald-700 cursor-pointer rounded-md"
                type="submit"
                value="Create Account"
            >
<!-- 
            <input
                class="px-5 py-2 bg-amber-700 cursor-pointer rounded-md"
                type="submit"
                value="Countinue as Guest"
            > -->
        </form>

    </div>
</body>

</html>